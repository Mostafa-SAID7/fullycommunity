name: Welcome

on:
  issues:
    types: [opened]
  pull_request_target:
    types: [opened]

permissions:
  issues: write
  pull-requests: write

jobs:
  welcome-issue:
    name: Welcome Issue Author
    runs-on: ubuntu-latest
    if: github.event_name == 'issues'
    steps:
      - name: Welcome first-time issue author
        uses: actions/first-interaction@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          issue-message: |
            👋 Welcome @${{ github.actor }}! Thanks for opening your first issue!
            
            We appreciate you taking the time to report this. Here's what happens next:
            
            1. 🏷️ A maintainer will review and label your issue
            2. 💬 We may ask for more information
            3. 🔧 Once confirmed, it will be prioritized
            
            **While you wait:**
            - Check our [Contributing Guide](CONTRIBUTING.md)
            - Review our [Code of Conduct](CODE_OF_CONDUCT.md)
            - Search for similar issues that might help
            
            Thank you for contributing to Fully Community! 🎉

  welcome-pr:
    name: Welcome PR Author
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request_target'
    steps:
      - name: Welcome first-time PR author
        uses: actions/first-interaction@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          pr-message: |
            👋 Welcome @${{ github.actor }}! Thanks for your first pull request!
            
            We're excited to review your contribution. Here's what happens next:
            
            1. 🤖 Automated checks will run (CI, linting, tests)
            2. 👀 A maintainer will review your code
            3. 💬 We may request changes or ask questions
            4. ✅ Once approved, your PR will be merged!
            
            **Checklist:**
            - [ ] Tests pass locally
            - [ ] Code follows our style guidelines
            - [ ] Documentation updated (if needed)
            
            Thank you for contributing to Fully Community! 🎉
